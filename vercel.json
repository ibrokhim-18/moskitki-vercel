{
  "version": 2,
  "buildCommand": "echo 'No build required'",
  "installCommand": "echo 'No install required'",
  "outputDirectory": "dist",
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" }
  ]
}
